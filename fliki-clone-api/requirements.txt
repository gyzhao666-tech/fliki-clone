# Web framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0          # async PostgreSQL driver
psycopg2-binary==2.9.10  # sync driver (alembic)

# Auth & security
python-jose[cryptography]==3.3.0
PyJWT==2.12.1
bcrypt==4.2.1
python-multipart==0.0.19  # form/file upload
# Fernet 对称加密（platform_credentials access/refresh token 落库加密 - Track-01）
# python-jose[cryptography] 已经间接依赖；这里显式声明便于审计 / pip-tools
cryptography>=41

# Config & validation
pydantic==2.10.3
pydantic-settings==2.6.1

# Storage (S3 / Cloudflare R2)
boto3==1.35.82
botocore==1.35.82

# Task queue
celery==5.4.0
redis==5.2.1

# HTTP client (OAuth, external APIs)
httpx==0.28.1

# Email
fastapi-mail==1.4.1

# Stripe
stripe==11.4.0

# PDF rendering（Track-22 月账单 invoice 渲染；轻量纯 Python；无 C 依赖）
# A4 page + 文字 logo + 表格 + 总金额；reportlab.platypus.SimpleDocTemplate 已经够用。
# >=4.0 是 pyproject 化版本（Python 3.10 兼容）。CI 强制安装。
reportlab>=4.0

# Utilities
python-dotenv==1.0.1
orjson==3.10.12     # fast JSON
pillow==11.0.0      # image processing

# Telegram crawler
PySocks==1.7.1
Telethon==1.43.2

# ── Optional: 本地 ASR (Track-06 fallback) ──────────────────────────────
# 体积大（CTranslate2 + 模型权重首次推理 ~150MB），仅当用户没有
# OPENAI_API_KEY 又想要 word-level 字幕时才需要安装；安装后 gateway ASR
# 路由自动落到 FasterWhisperLocalProvider。CI 不强制安装。
# 用 ">=" 而非锁版本，让 macOS / linux / cuda 用户各自挑合适 wheel。
faster-whisper>=1.0  # optional, see app/services/model_gateway/providers/faster_whisper_local.py
